## What this changes

Brief description of the change and the motivation.

Closes #

## How it was tested

- [ ] `npm run lint` passes
- [ ] `npm test` passes
- [ ] `npm run typecheck` passes
- [ ] Verified in the browser against the seeded demo data (`npm run seed`)

## Notes for reviewers

Anything non-obvious — schema/migration changes, accounting edge cases, etc.

> If this adds a migration, confirm it applies to an empty database
> (`npx prisma migrate deploy`) and preserves existing data.
